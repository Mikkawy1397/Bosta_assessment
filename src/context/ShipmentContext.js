import { createContext } from "react";

export const ShipmentContext = createContext({
  shipmentId: 67151313, // Default value for shipmentId
  setShipmentId: () => {}, // Placeholder for function to change shipmentId
});
